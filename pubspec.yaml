name: sp_settings
description: A Flutter settings screen package with Shared Preferences integration.
version: 1.1.0
repository: https://www.github.com/tytydraco/sp_settings

environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^3.0.1

flutter:
  uses-material-design: true
